[package]
name = "typebit-ffi"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "C ABI for the TypeBit engine (Kotlin/Swift/embedded bindings)."

[lib]
name = "typebit_ffi"
crate-type = ["cdylib", "staticlib", "lib"]

[dependencies]
typebit-core = { path = "../typebit-core" }

[features]
default = []
